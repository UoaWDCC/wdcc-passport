/**
 * Native size of the backdrop art (public/assets/pixel/scene-background.png). The shell draws
 * it with background-size: cover anchored top-centre; anything placed on the scene, and the
 * wide-screen surround, works in these units so it all lines up.
 */
export const SCENE_W = 360;
export const SCENE_H = 640;
