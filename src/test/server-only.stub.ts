// Test stub: the real `server-only` package throws when imported outside an RSC
// bundle. vitest.config.ts aliases `server-only` to this empty module so service
// files can be unit-tested in Node.
export {};
